def _extract(ips, ip):

    address = []

    if (ips == False):

        return(address)

    for _ in ips:
        
        (addr, max_time, retry_for_ip) = _

        if (addr == ip):

            address.append(_)

    return(address)

